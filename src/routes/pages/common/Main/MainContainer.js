import MainPresenter from './MainPresenter'


const MainContainer = () => {

    /* ===== RENDER ===== */
    return (
        <MainPresenter
        
        />
    );
};

export default MainContainer;