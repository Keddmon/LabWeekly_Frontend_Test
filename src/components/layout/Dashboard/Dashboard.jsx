import './Dashboard.style.css';

const Dashboard = ({
    className,

}) => {

    /* ===== RENDER ===== */
    return (
        <div className={className}>
            
        </div>
    );
};

export default Dashboard;